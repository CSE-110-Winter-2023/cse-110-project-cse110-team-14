package com.example.myapplication;
public class ZoomObserver {
    private int zoomLevel = 2;
    zoomIn(){
        check if zoomLevel == 1;
        if zoomLevel > 1{
            setRings(zoomLevel - 1);
        }
    }

    setRings(int zoomLevel){
        if 1:

        if 2:
        if 3:
        if 4:
    }
}
